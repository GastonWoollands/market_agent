import { PageHeader } from "@/components/PageHeader";

export default function OpportunitiesPage() {
  return (
    <PageHeader title="Opportunities">
      Quant scores first (cheap, quality, change, setup, insider, risk), then a short
      Claude memo on the top 15–25 names. No LLM scan of the full universe.
    </PageHeader>
  );
}
